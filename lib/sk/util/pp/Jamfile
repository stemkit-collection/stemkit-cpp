# vim: ft=jam: sw=4:

local library = sk.util.pp ;

local objects =
    AbstractNode.cc
    PrimeNode.cc
    PointerNode.cc
    # AttributesNode.cc
    # CollectionItemsNode.cc
    # CollectionNode.cc
    # HolderNode.cc
    # StructNode.cc
    TextNode.cc
;

make-static-library $(library) : $(objects) ;
make-subdirs test ;
