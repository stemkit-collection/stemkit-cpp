# vim: ft=jam: sw=2:

local library = sk.rt.event ;

local objects = 
  Dispatcher.cc
  PosixSignal.cc
  ExitingObserver.cc
  LoggingObserver.cc
  IgnoringObserver.cc
  ../Event.cc
;

make-shared-library $(library) : $(objects) ;
