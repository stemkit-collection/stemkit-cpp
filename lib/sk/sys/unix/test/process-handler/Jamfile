# vim: ft=jam: sw=2:

local program = process-handler ;

local objects = 
  app.cc
  Handler.cc
;

use-pthreads $(program) ;
use-project-libraries $(program) : sk.rt.bundle sk.rt.thread sk.rt.thread.pthreads sk.util ;
make-program $(program) : $(objects) ;
