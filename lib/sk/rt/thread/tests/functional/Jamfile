# vim: set sw=2:

local tests = 
  ticker 
  scope-logging 
  static-init
  mass-threads
  thread-demo
  scope-demo
;

local test ;
for test in $(tests) {
  local program = $(test) ;

  use-project-libraries $(program) : sk.rt.bundle sk.util sk.rt.thread ;
  make-program $(program) : $(test).cc ;
}
