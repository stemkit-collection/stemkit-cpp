# vim: set sw=2:

local test = sk-rt-thread-test ;

local objects = 
  TestSuite.cc
  ThreadTest.cc
  MutexTest.cc
  DispatcherTest.cc
;

local test-categories = mock pthreads ;
local category ;

for category in $(test-categories) {
  local test = $(test)-$(category) ;

  use-project-libraries $(test) : sk.util sk.cppunit sk.rt.thread ;
  use-project-libraries $(test) : sk.rt.thread.$(category) ;

  use-unit-test $(test) $(objects) ;
  make-cpp-test $(test) : $(objects) ;
}
